// Расширение «Бот_Интеграция». Заимствованная форма РегистрСведений.ПричиныОтменыЗаявок.ФормаЗаписи.
// Отключает старую отправку cancel-visit, которую прежний разработчик вписал прямо в основную
// конфигурацию: без секрета, только при слове «бот» в причине и с ложным «✅ сигнал отправлен».
// Теперь отмену отправляет модуль объекта заимствованной «Заявки». Основную конфигурацию не трогаем.
// Старый обработчик ПриЗаписиНаСервере содержал только код бота, поэтому заменяется пустым.

&НаСервере
&Вместо("ПриЗаписиНаСервере")
Процедура Бот_ПриЗаписиНаСервере(Отказ, ТекущийОбъект, ПараметрыЗаписи)
	Возврат;
КонецПроцедуры
