# План реворка 1CBOT

Дата: 2026-09-25 · Владелец: RAZORG777 · Основа: [конституция v2.0.0](../.specify/memory/constitution.md)

## Цель

Добавить новые возможности, исправить интеграцию с 1С (пациент + ЭМК) и обновить дизайн
WebApp, сохранив два независимых бота (Telegram и MAX).

## Принятые решения (сессия /grill-me)

| # | Вопрос | Решение |
|---|--------|---------|
| 1 | Цель реворка | Новые возможности + свежий дизайн |
| 2 | Новые возможности | Подтверждение визита в Telegram; исправить создание пациента в 1С (номер, ЭМК) |
| 3 | Архитектура | Полностью раздельно: 2 процесса, 2 порта (8001/8002), 2 БД, свой код у каждого бота |
| 4 | Репозиторий | Один репо, папки `telegram_bot/`, `max_bot/`, `onec/`, `docs/`; старые копии удаляются |
| 5 | Фронтенд | Vite + Vue 3 + TypeScript + Tailwind, отдельная сборка на каждый бот |
| 6 | Дизайн | Как сайт yasno-vizhu.com + светлая/тёмная тема мессенджера |
| 7 | 1С | БИТ.УМЦ 2.0.49.101, платформа 8.3.27; HTTP-сервис правит владелец; модуль выгружается в `onec/` |
| 8 | Подтверждение | Новый метод 1С меняет статус записи на «Подтверждена» (Telegram и MAX) |
| 9 | ПДн после визита | Хранить `PD_RETENTION_DAYS` (по умолчанию 30) дней, затем удалять; запись не блокирует новую |
| 10 | Порядок | Баги/безопасность → ЭМК → подтверждение → дизайн |
| 11 | Выкладка | CI (GitHub Actions) на PR + ручной `deploy.ps1` на Windows-сервере |
| 12 | Тестовый стенд | Тестовые боты + тестовая копия базы УМЦ уже есть |
| 13 | Одна запись на пациента | Ограничение остаётся (множественные записи не входят в реворк) |

## Найденные проблемы текущей версии

- **Безопасность:** WebApp доверяет `tg_id` из браузера — чужую запись можно посмотреть и отменить;
  вебхуки 1С и Telegram без аутентификации; пароль админки в коде; ПДн и сырые апдейты MAX в логах;
  согласие на ПДн дано фразой «нажимая кнопку…» без отдельной галочки, а Telegram и MAX ссылаются
  на разные документы политики.
- **Баг:** `finish-visit` не удаляет локальную запись, `tg_id` уникален → после визита пациент
  не может записаться снова (`SECOND_BOOKING_ERROR`).
- **1С:** телефон уходит как `+7 (999) 123-45-67`, дата рождения — `ДД.ММ.ГГГГ`; пациент не заводится
  карточкой с номером → нельзя оформить ЭМК.
- **Код:** 3–4 расходящиеся копии (корневой `main.py`, «— копия», `telegram_bot_test/`, `.bak`);
  дублирующийся роут `/schedule`; прокси Telegram зашит в код; `print` подменён для логов.
- **MAX:** кнопки «Подтверждаю / Отменить» в напоминаниях никогда не срабатывали (бот не подписан
  на `message_callback` и ищет payload не там); API переезжает на `platform-api2.max.ru`;
  форма открывается ссылкой с `?user_id=` вместо мини-приложения.
- **Git:** ничего не закоммичено, не было `.gitignore` — `.env`, `private.key` и БД с пациентами
  могли уйти в GitHub.

## Этапы

Каждый этап — отдельная фича Spec Kit и отдельная выкладка (сначала тестовый стенд, потом прод).

### Этап 0 — Гигиена, безопасность, блокирующие баги

Спецификация: [specs/001-security-hygiene/spec.md](../specs/001-security-hygiene/spec.md)

`/speckit-specify` —
> Гигиена репозитория и безопасность обоих ботов. Удалить старые копии (корневой main.py,
> «— копия», telegram_bot_test/, .bak, bot.py/test.py/check_db.py если не нужны). Проверка
> Telegram initData и подписи MAX на сервере вместо tg_id из запроса. Токен для вебхуков 1С,
> secret_token для вебхука Telegram, логин/пароль админки из .env. Убрать ПДн из логов.
> Отдельная галочка согласия на ПДн, не отмеченная по умолчанию. Исправить блокировку повторной записи после визита;
> ежедневная очистка по PD_RETENTION_DAYS. Прокси Telegram API в .env. Тесты с заглушкой 1С,
> GitHub Actions, deploy/deploy.ps1 и установка служб NSSM.

Готово, когда: CI зелёный; чужую запись нельзя получить подменой id; пациент после визита
записывается снова; в git нет секретов.

### Этап 1 — Пациент и ЭМК в 1С УМЦ

Модуль HTTP-сервиса выгружен в `onec/http-service.bsl`. Он уже создаёт клиента и медкарту,
но в обход стандартной логики УМЦ. Вероятные причины проблемы с ЭМК
(подробно — specs/001-security-hygiene/research.md › R16):

- клиент записывается с `ОбменДанными.Загрузка = Истина` — обработчики УМЦ не срабатывают,
  код (номер) пациента не присваивается;
- медкарта нумеруется вручную «максимум + 1», ошибки её создания молча глушатся;
- поиск клиента — точное ФИО + дата рождения без телефона; при нераспознанной дате —
  риск привязки к чужому клиенту.

`/speckit-specify` —
> При записи через бота метод book HTTP-сервиса БИТ.УМЦ должен находить карточку пациента
> по ФИО + дате рождения + телефону или создавать новую с автоматически присвоенным номером
> и привязывать к ней запись, чтобы регистратура могла оформить ЭМК. Боты передают телефон
> как +7XXXXXXXXXX и дату рождения как YYYY-MM-DD. Описать контракт в docs/onec-contract.md,
> код 1С — в onec/. Оба бота.

Готово, когда: на тестовой базе УМЦ запись из каждого бота создаёт или находит пациента
с номером, и по визиту открывается ЭМК; дублей пациентов не появляется.

### Этап 2 — Подтверждение визита

`/speckit-specify` —
> В напоминании за 24 часа кнопки «Подтверждаю» и «Отменить» в Telegram (как уже есть в MAX).
> «Подтверждаю» вызывает новый метод 1С confirm, который ставит записи статус «Подтверждена»;
> MAX переводится с update_note на тот же метод. Повторное нажатие идемпотентно.
> После отмены кнопки неактивны.

Готово, когда: статус в УМЦ меняется из обоих ботов; колл-центр видит подтверждённые записи.

### Этап 3 — Новый WebApp

`/speckit-specify` —
> Новый WebApp записи для обоих ботов на Vite + Vue 3 + TypeScript + Tailwind в стиле сайта
> yasno-vizhu.com: дизайн-токены с сайта, светлая/тёмная тема мессенджера, нативные кнопки
> Telegram (MainButton/BackButton) и аналоги MAX, шаги с прогрессом, экран «Моя запись».
> Отдельная сборка на каждый бот; без CDN в проде.

Перед реализацией — макеты экранов на согласование.

Готово, когда: макеты согласованы; сценарии записи, переноса и отмены проходят в обоих
мессенджерах на экранах от 320 px.

## Открытые вопросы

- TODO(PD_POLICY): сверить срок хранения и текст согласия с политикой ПДн клиники.
- Выгрузить в `onec/` код 1С, который отправляет `cancel-visit`/`finish-visit` (в HTTP-сервисе его нет).
- Какой документ политики ПДн единый для обоих ботов (`PD_POLICY_URL`): страница
  `yasno-vizhu.com/privacy-policy/` или PDF «Политика обработки ПД».
- Как MAX подписывает данные WebApp и вебхуки — уточнить по документации платформы на этапе 0.
